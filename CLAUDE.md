# CLAUDE.md — future-price

سجل حي للمشروع. اقرأه أولاً في أي جلسة جديدة، وحدّثه بعد كل خطوة مؤثرة.

## ما هو المشروع

منصة ثابتة (لا خادم، لا قاعدة بيانات، لا API) تحسب السعر العادل لاشتراك طفل في مركز رواد المستقبل
للأطفال من أسعار الباقات المعلنة. تُبنى بـNode المدمج وتُنشر على Cloudflare Pages.

- المستودع: `futurepioneers2030/future-price`
- الإنتاج: <https://future-price.pages.dev>
- المستودع الشقيق (صفحة الباقات): `futurepioneers2030/futurepioneers-baqat` — نفس `data/packages.json`
  ونفس الهوية البصرية.

## قواعد لا تُخرق

1. **`data/packages.json` هو مصدر الحقيقة الوحيد.** لا تكتب سعراً ولا رابط منتج في أي ملف `.js`
   أو `.mjs` أو `.css`. `src/verify.mjs` يفحص هذا آلياً ويفشل البناء إن خُرق.
2. **لا ترقية جزئية.** الشرائح المعلنة 4/5/6/8/10 ساعات فقط. من يطلب 7 ساعات يُحسب على 8،
   ومن يطلب 9 يُحسب على 10. ممنوع «8 ساعات + ساعة إضافية».
3. **لا اسم باقة لسعر غير موجود.** إذا فاز مسار الحساب بالساعة (`tier === null`) فالعنصر يُسمّى
   «اشتراك بالساعة» فقط، ولا يظهر له صف في جدول المقارنة.
4. **الأسعار متطابقة في الفترتين** الصباحية والمسائية — يختلف رابط المنتج فقط.
5. **لا هدف لمس أصغر من 44px**، وكل الألوان من رموز التصميم في `src/styles.css` حصراً.
6. الأداة الداخلية (`/`) **بلا اختيار فترة وبلا أزرار شراء** — هذا مطلب صريح من العميل.

## البنية والتدفق

`data/packages.json` + `data/discount.json` → `src/build.mjs` → `site/` (أربع صفحات + الأصول).
البيانات تُطبع وقت البناء داخل `<script type="application/json" id="rw-calc-data">`؛ لا جلب وقت التشغيل.
`src/calc.js` وحدة ES واحدة يستخدمها المتصفح و`src/verify.mjs` معاً — فما تختبره هو ما يعمل فعلاً.

عند تعطيل JavaScript (أو فشل تحميل الوحدة) تبقى محتويات `#rw-chat` / `#rw-calc` الأصلية ظاهرة:
جدول الأسعار الكامل للفترتين + رابط المتجر. الواجهة تمسحه عند الإقلاع.

## الحالة

- ✅ المحرك الحسابي + أمثلة التحقق الخمسة في كراسة التسليم.
- ✅ الأداة الداخلية بصيغة محادثة، بأربع خطوات ورجوع في كل خطوة (مكدّس `history` يقصّ الرسائل
  إلى `msgsLen` ويلغي كل المؤقتات المعلّقة — لا رسالة متأخرة ولا سؤال مكرر).
- ✅ نسخة الأهالي بعدّادات ومدة (أسبوع/شهر/ترم) وفترة وروابط شراء ومقارنة، وحالتها في الرابط.
- ✅ نسخة تضمين بلا ترويسة/تذييل.
- ✅ سعر الباقة يتناسب مع أيام الدوام (prorate): الأسعار المعلنة لدوام 5 أيام، و3 أيام = ⅗ السعر.
  ألغى تكرار السعر بين 3 و4 و5 أيام، وأبطل قاعدة «الحضور الجزئي بلا خصم» بطلب العميل.
  عمود 5 أيام مطابق للمعلن حرفياً، وأي سعر متناسب بلا رابط شراء (المتجر يبيع بالكامل).
- ✅ خصم الدوام المرن داخل قاعدة الأسعار الرئيسية (لا صفحة عروض منفصلة): من ساعاته لا تقابلها
  باقة معلنة (2/3/7/9) ويشترك شهرياً يُسعَّر بالأرخص من: سعر ساعة 16.25 (أقل 35%)، أو خصم
  15% على الباقة المرقّاة (packageOff — سقفه 16% وإلا انكسر ترتيب الأسعار). الباقات المعلنة والساعة الواحدة والأسبوع والترم بلا مساس. السياسة في data/discount.json.
- ✅ السطر المرن بلا رابط شراء — يظهر بزر «اطلب عبر واتساب» لأن السعر لا يقابله منتج في المتجر.
- ✅ 137 فحصاً في `npm run check` (منها مقارنة بمحرك بحث شامل مستقل، ومسح 12,800 تركيبة).
- ✅ منشورة على Cloudflare Pages.
- ⏳ سرّا `CLOUDFLARE_API_TOKEN` و`CLOUDFLARE_ACCOUNT_ID` يجب إضافتهما في GitHub ليعمل
  النشر التلقائي عند الدفع (النشر اليدوي عبر `npm run deploy` يعمل الآن).

## أوامر

```bash
npm run check    # بناء + 137 فحصاً — شغّله قبل أي دفع
npm run serve    # http://localhost:4173
npm run deploy   # نشر إنتاجي مباشر عبر wrangler
```

## إن أردت تعديل الحساب

كل المنطق في `src/calc.js` وحدها. أضف الحالة الجديدة كفحص في `src/verify.mjs` **قبل** التعديل،
ثم عدّل. لا تنسخ منطق الحساب إلى ملفات الواجهة.
